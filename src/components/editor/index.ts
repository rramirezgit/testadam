export { Editor } from './editor';
export { EditorRoot } from './styles';

export { editorClasses } from './classes';

// Versión unificada para migración
export { EditorUnified } from './editor-unified';

// Wrapper con feature flags
export { EditorWithFlags } from './editor-with-flags';

// Demo para comparación
export { default as EditorDemo } from './editor-demo';

export type { EditorProps } from './types';
